#include <stdio.h>
int main()
{
    char name[100], hobby[100];
    int age;
    scanf("%d %s %s", &age, &name, &hobby);
    printf("Name: %s\n", name);
    prinf("Age: %d\n", age);
    pintf("Hobby: %s\n", hobby);
    return 0;
}